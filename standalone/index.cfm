<cfscript>
	// writeDump( application.scheduler.getExecutor().getName() );
	// writeDump( application.scheduler.getRegisteredTasks() );

	// task = application.scheduler.getTaskRecord( "my-simple-task" );

	// if ( task.task.isDisabled() )
	// 	task.task.enable();
	// else
	// 	task.task.disable();

	// writeDump( task.task.isDisabled() );
</cfscript>